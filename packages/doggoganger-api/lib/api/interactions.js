export class Interactions {

  constructor(context) {
    this._context = context;
    this._options = context.options;
  }

  upload(records, { seed } = {}) {
    return [];
  }

  // TODO: delete

}
